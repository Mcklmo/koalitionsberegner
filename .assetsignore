# What `wrangler deploy` must never publish. Its assets directory is the repo
# root, and anything not listed here becomes a public URL — .env first of all,
# which holds the Anthropic key and the admin secret. Only index.html and js/ are the page.
.env*
.git
.gitignore
.dockerignore
.assetsignore
.claude
.vscode
.obsidian
.DS_Store
.pytest_cache
.wrangler
*.local.sh
Dockerfile
README.md
wrangler.jsonc
backend
worker
data
doc
test
node_modules
plugins
